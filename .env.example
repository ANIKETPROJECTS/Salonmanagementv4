# Copy this to `.env` on the VPS (never commit the real .env) and fill in real values.
# ecosystem.config.cjs loads this file at pm2 start time.
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?appName=SalonAdmin
PORT=3011
