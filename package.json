{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) command -v pnpm >/dev/null 2>&1 || npm install -g pnpm --quiet; pnpm install --no-frozen-lockfile ;; esac'",
    "build": "pnpm --filter @workspace/salon-app run build && pnpm --filter @workspace/api-server run build",
    "build:all": "pnpm -r run build",
    "start": "node artifacts/api-server/dist/index.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
